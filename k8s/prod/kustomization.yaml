apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml

replicas:
  - name: oncall-service
    count: 2
