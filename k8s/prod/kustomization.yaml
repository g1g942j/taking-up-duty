apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../base/deployment.yaml
  - ../base/service.yaml
  - configmap.yaml
  - secret.yaml

replicas:
  - name: oncall-service
    count: 2
